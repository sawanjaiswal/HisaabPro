Invoke the `hp-design` skill for HisaabPro UI work. Pass through any arguments as the design task.

Use the Skill tool: `skill: "hp-design"` with the user's arguments.

This is a shortcut to the HisaabPro design system skill that enforces brand tokens, component usage, border radius, typography, spacing, and all post-build checklist items.

$ARGUMENTS