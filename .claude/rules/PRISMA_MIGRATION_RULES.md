# Prisma Migrations

`db push` blocked by pre-tool-gate.sh · Schema without migration blocked by pre-commit
Drift: `npx prisma migrate dev --name X` · If stuck: manual SQL + `migrate resolve --applied`
GIN indexes (trgm): raw SQL only — no `@@index([name])` (duplicate B-tree)