/**
 * Public invoice routes — placeholder for PR3.
 * PR1 ships this file empty so the import compiles and the router mounts cleanly.
 */

import { Router } from 'express'

const router = Router()

// GET /api/p/invoice/:token — implemented in PR3
// router.get('/:token', publicRateLimiter('invoice'), resolvePublicToken('INVOICE'), handler)

export default router
