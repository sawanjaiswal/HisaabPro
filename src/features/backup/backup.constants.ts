/** Audit #5 — Google Drive backup feature constants. */

export const DRIVE_QUERY_KEY = ['backup', 'drive', 'status'] as const

/** Query-string flags the OAuth callback appends on redirect back to this page. */
export const CALLBACK_CONNECTED_PARAM = 'connected'
export const CALLBACK_ERROR_PARAM = 'error'
